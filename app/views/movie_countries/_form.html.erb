<%= form_for(movie_country) do |f| %>
  <% if movie_country.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(movie_country.errors.count, "error") %> prohibited this movie_country from being saved:</h2>

      <ul>
      <% movie_country.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
