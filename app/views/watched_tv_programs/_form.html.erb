<%= form_for(watched_tv_program) do |f| %>
  <% if watched_tv_program.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(watched_tv_program.errors.count, "error") %> prohibited this watched_tv_program from being saved:</h2>

      <ul>
      <% watched_tv_program.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
